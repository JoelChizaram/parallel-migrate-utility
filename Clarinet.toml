[project]
name = "parallel-migrate-utility"
authors = ["Anonymous"]
description = "A blockchain utility for secure, distributed data migration and tracking"
telemetry = false
requirements = []
[contracts.parallel-migrate]
path = "contracts/parallel-migrate.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
